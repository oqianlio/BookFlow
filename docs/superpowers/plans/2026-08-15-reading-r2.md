# 阅读体验 R2：切片翻页引擎（书源正文）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为书源正文引入 legado 式切片翻页引擎（按高度切分 HTML 为独立页），支持 scroll/cover/slide 模式与页进度。

**Architecture:** 新建 `src/readers/PaginatedReader.tsx`（切片算法 + 翻页渲染）；统一 ReaderPage 书源正文渲染 `<PaginatedReader html={content} mode="scroll" ... />`；TXT/MD/EPUB/PDF 保持现有渲染。

**Tech Stack:** React 19 + TypeScript + vitest（jsdom）。无新依赖。

## Global Constraints

- 仅书源正文接入切片翻页；TXT/MD/EPUB/PDF 保持现有渲染（不动）。
- 现有测试保持绿：`npm test`（当前 229），`npm run build`（tsc + vite）通过。
- Shell 为 PowerShell 7；测试命令 `npx vitest run <file>`；不修改 `docs/` 与 `.git/`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/readers/PaginatedReader.tsx` | 切片翻页引擎 | 新建 |
| `src/readers/PaginatedReader.test.tsx` | 切片/翻页测试 | 新建 |
| `src/pages/ReaderPage.tsx` | 书源正文接 PaginatedReader | 修改 |
| `src/pages/ReaderPage.css` | 切片样式 | 修改 |

## 任务依赖

Task 1（切片算法）→ Task 2（组件渲染/翻页）→ Task 3（接入 ReaderPage）。

---

### Task 1: 切片算法（纯函数）

**Files:**
- Create: `src/readers/PaginatedReader.tsx`（先实现 `sliceHtmlIntoPages` 导出）
- Test: `src/readers/PaginatedReader.test.tsx`

**Interfaces:**
- Produces:
  ```ts
  export function sliceHtmlIntoPages(html: string, pageHeightPx: number, measureWidthPx: number): string[];
  ```
  纯函数：把 html 的块级元素（p/div/h1-h6/li/pre）按高度切片为页数组（每页 html 字符串）。

- [ ] **Step 1: 写失败测试**

```ts
// src/readers/PaginatedReader.test.tsx
import { describe, it, expect } from "vitest";
import { sliceHtmlIntoPages } from "./PaginatedReader";

const HEIGHT = 300; // 模拟阅读区高

// 用一个可预测的测量函数替代真实 DOM 测量（避免 jsdom 布局不确定性）：
// 简化：每 10 个字符占一行，每行 15px，300px = 20 行 = 200 字符/页。
function mockSlice(html: string): string[] {
  // 提取 <p>...</p> 文本，每 200 字符切一页，保留 p 标签
  const ps = Array.from(html.matchAll(/<p>(.*?)<\/p>/gs)).map((m) => m[1]);
  const pages: string[] = [];
  let cur = "";
  let count = 0;
  for (const t of ps) {
    if (count + t.length > 200 && cur) { pages.push(cur); cur = ""; count = 0; }
    cur += `<p>${t}</p>`;
    count += t.length;
  }
  if (cur) pages.push(cur);
  return pages;
}

describe("sliceHtmlIntoPages", () => {
  it("returns one page for short content", () => {
    const html = "<p>短内容</p>";
    const pages = sliceHtmlIntoPages(html, HEIGHT, 400);
    expect(pages.length).toBe(1);
    expect(pages[0]).toContain("短内容");
  });

  it("splits long content into multiple pages preserving paragraph boundaries", () => {
    // 200 字符/页，300 字符 → 2 页
    const long = Array.from({ length: 30 }, (_, i) => `<p>段落${i}${"字".repeat(9)}</p>`).join(""); // 每段 13 字符,30 段=390
    const pages = sliceHtmlIntoPages(long, HEIGHT, 400);
    expect(pages.length).toBeGreaterThan(1);
    // 每页不含被截断的半段（段落在页边界保留）
    for (const p of pages) expect(p.trim().startsWith("<p>")).toBe(true);
  });

  it("handles a single paragraph taller than one page by overflowing", () => {
    const huge = `<p>${"长".repeat(500)}</p>`; // 500 字符 > 200/页
    const pages = sliceHtmlIntoPages(huge, HEIGHT, 400);
    expect(pages.length).toBe(1); // 超页段落不截断，单页溢出
  });

  it("returns empty array for empty input", () => {
    expect(sliceHtmlIntoPages("", HEIGHT, 400)).toEqual([]);
  });
});
```

注意：由于 jsdom 无真实布局，`sliceHtmlIntoPages` 的真实测量需在浏览器。**测试策略**：测试用 `mockSlice`（可预测的纯逻辑）验证**接口约定**，而真实实现用 DOM 测量（`ResizeObserver`/`getBoundingClientRect`）。为让测试通过，`sliceHtmlIntoPages` 实现为可注入测量函数的版本：

```ts
export function sliceHtmlIntoPages(html: string, pageHeightPx: number, measureWidthPx: number): string[];
```

若实现依赖 DOM，测试注入 `measureTextWidth`/`measureHeight` 回调。**方案**：`sliceHtmlIntoPages` 接受可选的第四参 `measure?: (html: string) => { height: number }`（默认用真实 DOM 测量容器），测试传 `mockMeasure` 返回 `(文本长度/10) * 15`。以实际运行为准，保证 4 测试通过。

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/readers/PaginatedReader.test.tsx`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 sliceHtmlIntoPages**

```ts
// src/readers/PaginatedReader.tsx
import { useEffect, useRef, useState, useCallback } from "react";

export type PageMode = "scroll" | "cover" | "slide";

export function sliceHtmlIntoPages(
  html: string,
  pageHeightPx: number,
  measureWidthPx: number,
  measure?: (h: string) => number,
): string[] {
  if (!html.trim()) return [];
  // 1. 拆分块级片段（p/div/h1-h6/li/pre），保留标签
  const blocks = Array.from(html.matchAll(/<(p|div|h[1-6]|li|pre)[^>]*>[\s\S]*?<\/\1>/g)).map((m) => m[0]);
  if (blocks.length === 0) return [html]; // 无块级 → 整篇一页
  // 2. 逐块加入测量，高度超页则成页（默认真实测量由组件提供，此处用传入 measure）
  const m = measure ?? defaultMeasure;
  const pages: string[] = [];
  let cur = "";
  for (const b of blocks) {
    const h = m((cur + b));
    if (h > pageHeightPx && cur) {
      pages.push(cur);
      cur = "";
    }
    cur += b;
  }
  if (cur) pages.push(cur);
  return pages.length ? pages : [html];
}

function defaultMeasure(_h: string): number {
  // 兜底：按字符数估算（每 10 字符 15px）；真实测量由组件内 measurement 容器覆盖
  return (_h.length / 10) * 15;
}
```

（真实组件内用隐藏容器 `getBoundingClientRect().height` 作为 measure，见 Task 2。）

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/readers/PaginatedReader.test.tsx`
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add src/readers/PaginatedReader.tsx src/readers/PaginatedReader.test.tsx
git commit -m "feat: 切片翻页算法"
```

---

### Task 2: PaginatedReader 组件（渲染 + 翻页）

**Files:**
- Modify: `src/readers/PaginatedReader.tsx`（加组件）
- Test: `src/readers/PaginatedReader.test.tsx`（加组件测试）

**Interfaces:**
- Consumes: `sliceHtmlIntoPages`（Task 1）；`PageMode`。
- Produces:
  ```ts
  export default function PaginatedReader({
    html, mode = "scroll", fontSizePx = 18, onPageChange,
  }: {
    html: string; mode?: PageMode; fontSizePx?: number;
    onPageChange?: (cur: number, total: number) => void;
  });
  ```

- [ ] **Step 1: 写失败测试（追加）**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import PaginatedReader, { sliceHtmlIntoPages } from "./PaginatedReader";

describe("PaginatedReader", () => {
  it("renders current page and reports progress", () => {
    const html = Array.from({ length: 30 }, (_, i) => `<p>段落${i}${"字".repeat(9)}</p>`).join("");
    const onPageChange = vi.fn();
    render(<PaginatedReader html={html} mode="scroll" onPageChange={onPageChange} />);
    // 首页渲染，onPageChange 被调 cur=0, total>1
    expect(onPageChange).toHaveBeenCalledWith(0, expect.any(Number));
  });

  it("next/prev navigates pages and region click flips", () => {
    const html = Array.from({ length: 30 }, (_, i) => `<p>段落${i}${"字".repeat(9)}</p>`).join("");
    const { container } = render(<PaginatedReader html={html} mode="cover" />);
    const wrap = container.querySelector(".reader-slice-wrap")!;
    // 点击右 1/3 → next
    fireEvent.click(wrap, { clientX: 900 });
    expect(wrap.textContent).toContain("段落"); // 第 2 页有内容
    // 点击左 1/3 → prev
    fireEvent.click(wrap, { clientX: 100 });
    expect(wrap.textContent).toContain("段落0");
  });

  it("renders empty state for empty html", () => {
    render(<PaginatedReader html="" />);
    expect(screen.getByText(/无内容/)).toBeInTheDocument();
  });
});
```

（若 jsdom 布局不可靠导致切片数不符合预期，用 `sliceHtmlIntoPages(html, HEIGHT, 400, mockMeasure)` 显式注入 measure——组件支持 `measure` 注入供测试。）

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/readers/PaginatedReader.test.tsx`
Expected: FAIL（组件未实现）

- [ ] **Step 3: 实现组件**

```tsx
export default function PaginatedReader({ html, mode = "scroll", fontSizePx = 18, onPageChange, measure }: {
  html: string; mode?: PageMode; fontSizePx?: number;
  onPageChange?: (cur: number, total: number) => void;
  measure?: (h: string) => number;
}) {
  const wrapRef = useRef<HTMLDivElement>(null);
  const [pages, setPages] = useState<string[]>([]);
  const [page, setPage] = useState(0);

  // 真实测量：隐藏容器
  const measureRef = useRef(measure);
  measureRef.current = measure;
  const realMeasure = useCallback((h: string): number => {
    if (measureRef.current) return measureRef.current(h);
    const el = document.createElement("div");
    el.style.cssText = `position:absolute;visibility:hidden;width:${wrapRef.current?.clientWidth ?? 400}px;font-size:${fontSizePx}px;white-space:normal;`;
    el.innerHTML = h;
    document.body.appendChild(el);
    const height = el.getBoundingClientRect().height;
    document.body.removeChild(el);
    return height;
  }, [fontSizePx]);

  useEffect(() => {
    const h = wrapRef.current?.clientHeight ?? 500;
    const w = wrapRef.current?.clientWidth ?? 400;
    setPages(sliceHtmlIntoPages(html, h, w, realMeasure));
    setPage(0);
  }, [html, realMeasure]);

  const total = pages.length;
  const go = (p: number) => {
    const c = Math.min(Math.max(0, p), total - 1);
    setPage(c);
    onPageChange?.(c, total);
  };

  useEffect(() => { onPageChange?.(0, total); }, [total]); // 初始上报

  const handleClick = (e: React.MouseEvent) => {
    const rect = (e.currentTarget as HTMLElement).getBoundingClientRect();
    const x = e.clientX - rect.left;
    const third = rect.width / 3;
    if (x < third) go(page - 1);
    else if (x > third * 2) go(page + 1);
    else onMenuToggle?.();  // 中区 → 呼出菜单（由 ReaderPage 传入）
  };

  if (!html.trim()) return <p className="panel-empty">无内容</p>;

  return (
    <div className="reader-slice-wrap" ref={wrapRef} onClick={handleClick} style={{ fontSize: fontSizePx }}>
      {pages.map((p, i) => (
        <div
          key={i}
          className={`reader-page-slice${i === page ? " active" : ""}${mode === "slide" ? " slide" : ""}`}
          style={{ display: i === page ? "block" : "none" }}
          dangerouslySetInnerHTML={{ __html: p }}
        />
      ))}
      <div className="reader-slice-nav">
        <button onClick={(e) => { e.stopPropagation(); go(page - 1); }} disabled={page === 0}>‹</button>
        <span>{total ? `${page + 1} / ${total}` : "0 / 0"}</span>
        <button onClick={(e) => { e.stopPropagation(); go(page + 1); }} disabled={page >= total - 1}>›</button>
      </div>
    </div>
  );
}
```

（组件加 `onMenuToggle?: () => void` prop 供中区点击呼出菜单。）

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/readers/PaginatedReader.test.tsx`
Expected: PASS（含 Task 1 的 4 + 本批 3）

- [ ] **Step 5: Commit**

```bash
git add src/readers/PaginatedReader.tsx src/readers/PaginatedReader.test.tsx
git commit -m "feat: 切片翻页组件（scroll/cover/slide）"
```

---

### Task 3: 接入统一 ReaderPage（书源正文）

**Files:**
- Modify: `src/pages/ReaderPage.tsx`
- Modify: `src/pages/ReaderPage.css`

**Interfaces:**
- Consumes: `PaginatedReader` from `../readers/PaginatedReader`。
- Produces: 无新接口。

- [ ] **Step 1: ReaderPage 书源正文接 PaginatedReader**

`src/pages/ReaderPage.tsx` 书源正文分支（原 `md-content` 内联 HTML，line ~315）：

```tsx
) : chapter.url ? (
  <PaginatedReader
    html={content}
    mode="scroll"
    onMenuToggle={() => setMenuVisible((v) => !v)}
    onPageChange={(cur, total) => {
      // 进度：更新底部章节进度（可选，R2 先用页进度）
    }}
  />
) : (
```

import `PaginatedReader` from `../readers/PaginatedReader`。移除 `md-content` 内联 HTML 渲染（改走 PaginatedReader）。

注意：`main` 的 `onClick` 已切换 menuVisible——PaginatedReader 内部 click 会冒泡。**协调**：移除 `main` 的 onClick（改由 PaginatedReader 的 handleClick 处理：左/右翻页、中区 onMenuToggle）。`main onClick` 删除，`main` 不再自己 toggle menu。

- [ ] **Step 2: 样式（ReaderPage.css 追加）**

```css
.reader-slice-wrap { position: relative; height: 100%; overflow: hidden; }
.reader-page-slice { position: absolute; inset: 0; overflow: hidden; padding: 0 4px; }
.reader-page-slice > * { margin: 0; }
.reader-page-slice.slide { transition: transform 0.3s ease; }
.reader-slice-nav {
  position: absolute; bottom: 8px; right: 12px; display: flex; gap: 8px; align-items: center;
  background: var(--surface-2); border: 1px solid var(--border); border-radius: var(--radius-sm);
  padding: 2px 8px; font-size: 12px; color: var(--fg-muted);
}
.reader-slice-nav button { border: none; background: transparent; color: var(--fg); font-size: 14px; cursor: pointer; }
```

- [ ] **Step 3: 测试**

- ReaderPage.source.test.tsx 现有正文断言（`第一章正文内容。` 等）——PaginatedReader 切片后仍应渲染该文本（首页含）。若切片逻辑导致文本被分页，用 `sliceHtmlIntoPages` 的段落保留保证首页含首段。以实际运行修正。
- PaginatedReader 测试保持绿。
- `npm test` 全绿、`npm run build` 通过。

- [ ] **Step 4: 终审清单**

- [ ] `sliceHtmlIntoPages` 纯函数 + 4 测试 ✓
- [ ] PaginatedReader 组件（scroll/cover/slide、区域翻页、进度、空态）✓
- [ ] ReaderPage 书源正文接 PaginatedReader，main onClick 协调 ✓
- [ ] TXT/MD/EPUB/PDF 未改动 ✓
- [ ] `npm test` 全绿、`npm run build` 通过、工作树干净 ✓

若遗漏立即修复并补 commit（`fix: 切片翻页终审修复`）。

- [ ] **Step 5: Commit**

```bash
git add src/pages/ReaderPage.tsx src/pages/ReaderPage.css
git commit -m "feat: 书源正文接入切片翻页"
```

---
