# 阅读体验 R1：统一阅读外壳实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立统一 legado 式阅读外壳，本地书与书源共用同一 ReaderPage，保留现有渲染组件。

**Architecture:** 新建 `src/services/reading.ts`（ReaderSource 类型）；重构 `src/pages/ReaderPage.tsx` 为统一外壳（按 source.kind 分发到 local reader 或 source 阅读逻辑）；删除 SourceReaderPage；App.tsx 两分支渲染统一页。

**Tech Stack:** React 19 + TypeScript + vitest（jsdom）。无新依赖。

## Global Constraints

- 不改渲染组件逻辑（Epub/Pdf/Md/Txt/MangaViewer）。
- 保留书源阅读的章节/进度/保存/漫画/重试/登录逻辑。
- 现有测试保持绿：`npm test`（当前 226），`npm run build`（tsc + vite）通过。
- Shell 为 PowerShell 7；测试命令 `npx vitest run <file>`；不修改 `docs/` 与 `.git/`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/services/reading.ts` | ReaderSource 类型 | 新建 |
| `src/pages/ReaderPage.tsx` | 统一阅读外壳 | 重构 |
| `src/pages/SourceReaderPage.tsx` | 删除（逻辑并入） | 删除 |
| `src/App.tsx` | 两分支渲染统一页 | 修改 |
| `src/pages/ReaderPage.css` | 底部工具栏样式 | 修改 |
| `src/pages/ReaderPage.test.tsx` | local 分支测试 | 更新 |
| `src/pages/SourceReaderPage.test.tsx` | → 迁移为统一页 source 分支测试 | 重命名/更新 |

## 任务依赖

Task 1（ReaderSource 类型 + 统一 ReaderPage 外壳 + App 接线）→ Task 2（测试迁移 + 终审）。

---

### Task 1: 统一 ReaderPage 外壳

**Files:**
- Create: `src/services/reading.ts`
- Modify: `src/pages/ReaderPage.tsx`
- Delete: `src/pages/SourceReaderPage.tsx`
- Modify: `src/App.tsx`
- Modify: `src/pages/ReaderPage.css`

**Interfaces:**
- Consumes: `Book`（api）；`EpubReader`/`PdfReader`/`MdReader`/`TxtReader`/`MangaViewer`（现有）；书源阅读 API（`listBookSources`/`httpGet`/`getBookSourceProgress`/`saveBookSourceProgress`/`mergeUserAgent`/`openLoginWindow`）；`useError`。
- Produces:
  ```ts
  // src/services/reading.ts
  export type ReaderSource =
    | { kind: "local"; book: Book }
    | { kind: "source"; sourceId: number; bookUrl: string; bookTitle: string;
        chapterIndex: number; chapterUrl: string; chapterName: string };
  // ReaderPage.tsx
  export default function ReaderPage({ source, onBack }: { source: ReaderSource; onBack: () => void });
  ```

- [ ] **Step 1: 新建 reading.ts**

```ts
// src/services/reading.ts
import type { Book } from "./api";

export type ReaderSource =
  | { kind: "local"; book: Book }
  | { kind: "source"; sourceId: number; bookUrl: string; bookTitle: string;
      chapterIndex: number; chapterUrl: string; chapterName: string };
```

- [ ] **Step 2: 重构 ReaderPage 为统一外壳**

`src/pages/ReaderPage.tsx` 全量重构，保留现有两套逻辑：

```tsx
import { useCallback, useEffect, useRef, useState } from "react";
import EpubReader from "../readers/EpubReader";
import PdfReader from "../readers/PdfReader";
import MdReader from "../readers/MdReader";
import TxtReader from "../readers/TxtReader";
import MangaViewer from "../readers/MangaViewer";
import AnnotationPanel from "../components/AnnotationPanel";
import BookmarkPanel from "../components/BookmarkPanel";
import TtsBar from "../components/TtsBar";
import { BackIcon, BookmarkIcon, HighlightIcon } from "../components/icons";
import { addBookmark, removeBook, httpGet, listBookSources, getBookSourceProgress, saveBookSourceProgress, mergeUserAgent, openLoginWindow } from "../services/api";
import { parseBookSourceJson, parseHtml, extractSingle, purifyContent, isImageChapter, extractImageUrls, type BookSource as Src } from "../services/bookSourceEngine";
import { useError } from "../components/ErrorDialog";
import { type ReaderSource } from "../services/reading";
import "./ReaderPage.css";

interface ChapterState { index: number; url: string; name: string }

export default function ReaderPage({ source, onBack }: { source: ReaderSource; onBack: () => void }) {
  const isLocal = source.kind === "local";
  const book = isLocal ? source.book : null;
  // ==== 通用 ====
  const [panel, setPanel] = useState<"annotations" | "bookmarks" | null>(null);
  const [openError, setOpenError] = useState<string | null>(null);
  const [menuVisible, setMenuVisible] = useState(true);
  const { showError } = useError();
  const jumpKey = useRef(0);

  // ==== 书源阅读状态 ====
  const [chapter, setChapter] = useState<ChapterState>({
    index: isLocal ? -1 : source.chapterIndex,
    url: isLocal ? "" : source.chapterUrl,
    name: isLocal ? "" : source.chapterName,
  });
  const [content, setContent] = useState("");
  const [images, setImages] = useState<string[]>([]);
  const [isManga, setIsManga] = useState(false);
  const [loading, setLoading] = useState(!isLocal);
  const [failed, setFailed] = useState(false);
  const [src, setSrc] = useState<Src | null>(null);
  const nextUrlRef = useRef("");
  const prevUrlsRef = useRef<string[]>([]);
  const saveTimer = useRef<number | null>(null);
  const chapterRef = useRef(chapter);
  chapterRef.current = chapter;

  // ==== 书源：加载章节（现有 loadChapter 逻辑原样迁入）====
  const loadChapter = useCallback(async (c: ChapterState) => {
    if (!isLocal && c.url) {
      // ...（SourceReaderPage 现有 loadChapter 逻辑：setFailed/loading、listBookSources、httpGet、extractSingle、isImageChapter、purifyContent）
    }
  }, [source.kind, isLocal, source.sourceId, showError]);

  // ==== 书源：进度恢复/保存（现有逻辑）====

  // ==== 本地书：reader 渲染分发 ====

  // ==== 书源：目录/上一章/下一章/登录（现有逻辑）====

  return (
    <div className="reader-page">
      <header className={`reader-toolbar${menuVisible ? "" : " reader-toolbar-hidden"}`}>
        <button className="btn-icon" onClick={onBack} aria-label="返回" title="返回"><BackIcon size={18} /></button>
        <h2>
          {isLocal ? book!.title : (
            <span className="reader-title">{source.bookTitle}</span>
          )}
          {!isLocal && chapter.name && <> · <span className="reader-chapter">{chapter.name}</span></>}
        </h2>
        <div className="toolbar-actions">
          {isLocal ? (
            <>
              <TtsBar />
              <button className={`btn-icon${panel === "annotations" ? " active" : ""}`} onClick={() => setPanel((p) => (p === "annotations" ? null : "annotations"))} aria-label="标注" title="标注"><HighlightIcon size={17} /></button>
              <button className={`btn-icon${panel === "bookmarks" ? " active" : ""}`} onClick={() => setPanel((p) => (p === "bookmarks" ? null : "bookmarks"))} aria-label="书签" title="书签"><BookmarkIcon size={17} /></button>
            </>
          ) : (
            <>
              {src?.loginUrl && <button className="btn btn-ghost" onClick={() => { /* 登录逻辑 */ }}>登录</button>}
            </>
          )}
        </div>
      </header>
      <div className="reader-body">
        <main
          className="reader-main"
          onClick={() => setMenuVisible((v) => !v)}
        >
          {/* local: 按 format 分发（现有 ReaderPage 逻辑 + error 处理） */}
          {/* source: 加载/失败重试/正文/漫画（现有 SourceReaderPage 逻辑） */}
        </main>
        {isLocal && panel === "annotations" && <AnnotationPanel bookId={book!.id} format={book!.format} onJump={jump} onChanged={() => jumpKey.current += 1} />}
        {isLocal && panel === "bookmarks" && <BookmarkPanel bookId={book!.id} onJump={jump} onChanged={() => jumpKey.current += 1} />}
      </div>
      {!isLocal && (
        <footer className="reader-bottom-bar">
          <button className="btn btn-ghost" onClick={() => goChapter(-1)} disabled={loading || prevUrlsRef.current.length === 0}>上一章</button>
          <span className="reader-progress">第 {chapter.index + 1} 章</span>
          <button className="btn btn-ghost" onClick={() => goChapter(1)} disabled={!!loading || failed || !nextUrlRef.current}>下一章</button>
        </footer>
      )}
    </div>
  );
}
```

关键：把 SourceReaderPage 的 `loadChapter`/`goChapter`/`persist`/进度恢复逻辑**原样迁入**统一 ReaderPage（仅在 `!isLocal` 分支生效），本地书分支沿用 ReaderPage 现有逻辑（键盘快捷键、标注/书签面板、搜索跳转）。

- [ ] **Step 3: 删除 SourceReaderPage.tsx**

`git rm src/pages/SourceReaderPage.tsx`（逻辑已迁入统一页）。

- [ ] **Step 4: App.tsx 接线**

```tsx
case "reader":
  return <ReaderPage source={{ kind: "local", book: state.book }} onBack={() => go(state.back)} />;
case "sourceReader":
  return <ReaderPage
    source={{
      kind: "source",
      sourceId: state.sourceId,
      bookUrl: state.bookUrl,
      bookTitle: state.bookTitle,
      chapterIndex: state.chapterIndex,
      chapterUrl: state.chapterUrl,
      chapterName: state.chapterName,
    }}
    onBack={() => { /* 现有 sourceReader 返回逻辑 */ }}
  />;
```

import 移除 `SourceReaderPage`。

- [ ] **Step 5: 底部工具栏样式（ReaderPage.css 追加）**

```css
.reader-toolbar-hidden { transform: translateY(-100%); }
.reader-toolbar { transition: transform 0.2s ease; }
.reader-bottom-bar {
  display: flex; align-items: center; justify-content: space-between; gap: 10px;
  padding: 8px 18px; border-top: 1px solid var(--border);
  background: var(--surface-2); flex-shrink: 0;
}
.reader-progress { font-size: 12.5px; color: var(--fg-muted); font-variant-numeric: tabular-nums; }
```

（注：ReaderPage.css 用纸张 token，`--border`/`--surface-2`/`--fg-muted` 在 `.reader-page` 作用域已有。）

- [ ] **Step 6: 构建 + 编译验证**

Run: `npm run build` 通过（tsc 会暴露漏迁移的引用）。`npm test` 现有测试会红（ReaderPage/SourceReaderPage 测试待迁移）——本任务只保证 build 过 + 不删既有测试断言，测试迁移在 Task 2。

- [ ] **Step 7: Commit**

```bash
git add src/services/reading.ts src/pages/ReaderPage.tsx src/pages/SourceReaderPage.tsx src/App.tsx src/pages/ReaderPage.css
git commit -m "feat: 统一阅读外壳（本地书+书源）"
```

---

### Task 2: 测试迁移 + 终审

**Files:**
- Modify: `src/pages/ReaderPage.test.tsx`（local 分支）
- Modify: `src/pages/SourceReaderPage.test.tsx` → `src/pages/ReaderPage.source.test.tsx`（source 分支，或并入 ReaderPage.test.tsx）
- Modify: `src/App.test.tsx`（如 reader 相关断言）

**Interfaces:**
- Consumes: Task 1 的统一 ReaderPage（`source` prop）。

- [ ] **Step 1: 迁移 local 分支测试**

`ReaderPage.test.tsx` 现有渲染 `render(<ReaderPage book={...} onBack={...} />)` → 改 `render(<ReaderPage source={{ kind: "local", book }} onBack={...} />)`。断言不变（reader mock、返回、TTS、标注/书签、error）。

- [ ] **Step 2: 迁移 source 分支测试**

`SourceReaderPage.test.tsx` 现有渲染 `render(<SourceReaderPage sourceId={...} ... onBack={...} />)` → 改 `render(<ReaderPage source={{ kind: "source", sourceId, bookUrl, bookTitle, chapterIndex, chapterUrl, chapterName }} onBack={...} />)`。断言不变（章节加载/正文/漫画/重试/登录/进度恢复/下一章禁用）。文件重命名或并入 ReaderPage.test.tsx（按实际运行，避免 import 冲突）。

- [ ] **Step 3: 运行测试 + 构建**

Run: `npx vitest run src/pages/ReaderPage.test.tsx src/pages/SourceReaderPage.test.tsx src/App.test.tsx` PASS；`npm test` 全绿；`npm run build` 通过。

- [ ] **Step 4: 终审清单**

- [ ] `reading.ts` ReaderSource 类型导出 ✓
- [ ] 统一 ReaderPage 接收 `source` prop，local/source 分发正确 ✓
- [ ] SourceReaderPage 已删除，App 两分支用统一页 ✓
- [ ] 书源章节/进度/漫画/重试/登录逻辑保留 ✓
- [ ] 本地书 reader/标注/书签/TTS/error 逻辑保留 ✓
- [ ] 底部工具栏（source）+ 点击呼出/隐藏 ✓
- [ ] `npm test` 全绿、`npm run build` 通过、工作树干净 ✓

若遗漏立即修复并补 commit（`fix: 阅读外壳终审修复`）。

- [ ] **Step 5: Commit**

```bash
git add src/pages/ReaderPage.test.tsx src/pages/SourceReaderPage.test.tsx src/App.test.tsx
git commit -m "test: 统一阅读页测试迁移"
```

---
