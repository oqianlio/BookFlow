# 「枕书」MD3 界面重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将「枕书」重构为参照 legado 3.0（Material Design 3）的视觉与导航风格：5 目的地侧边导航、多套可切换 MD3 配色、MD3 组件样式，同时保持阅读页纸张体验不变。

**Architecture:** `theme.ts` 重构为「配色方案 × 明暗」矩阵（DOM 用 `data-scheme` + `data-mode` 双属性）；`App.tsx` 重构为「主区 area + 全屏 detail」导航模型，主区外层挂持久侧边栏；App.css 全面替换为 MD3 语义 token 与组件样式；新增 HomePage（统计+最近阅读）与 RssPage（占位）。

**Tech Stack:** React 19 + TypeScript + Vite 7 + vitest（jsdom）。无新依赖。

## Global Constraints

- 阅读页（ReaderPage + ReaderPage.css）保持纸张风格：ReaderPage.css 顶部新增 `.reader-page` 作用域内的纸张 token 定义，不得依赖全局 MD3 token。
- 不新增 Rust 后端命令：首页统计全部由 `listBooks()`（含 `last_opened_at`，秒级 Unix 时间戳）推导。
- 不引入路由库：导航继续用 React `useState` 状态模型。
- 主题持久化格式：`settings` 表 `theme` = `"sora:light"`；兼容旧值 `"light"`/`"dark"`（解析为默认 sora 方案 + 该模式）。
- DOM 属性：`applyTheme` 同时设置 `data-scheme`（如 `"sora"`）与 `data-mode`（`"light"`|`"dark"`），替代原 `data-theme`。
- 现有测试保持绿：`npm test`（当前 170），`npm run build`（tsc + vite build）通过。
- Shell 为 PowerShell 7；测试命令 `npx vitest run <file>`；不修改 `docs/` 与 `.git/`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/components/theme.ts` | 配色矩阵、parse/apply/set/get/init | 重构 |
| `src/components/theme.test.ts` | token 矩阵与解析测试 | 重构 |
| `src/App.css` | MD3 token + 侧边栏/卡片/按钮/列表样式 | 重构 |
| `src/pages/ReaderPage.css` | 纸张 token 作用域隔离 | 修改 |
| `src/components/SideNav.tsx` | 侧边导航栏 | 新建 |
| `src/components/icons.tsx` | 增加 Home/Rss 图标 | 修改 |
| `src/pages/HomePage.tsx` | 首页（统计+最近阅读） | 新建 |
| `src/pages/RssPage.tsx` | RSS 占位页 | 新建 |
| `src/App.tsx` | AppState 导航模型 + 外壳 | 重构 |
| `src/pages/SettingsPage.tsx` | → 我的页（单列设置中心+配色方案） | 重构 |
| 各页面组件 | 移除返回按钮/顶部导航，套 MD3 类 | 修改 |

## 任务依赖

Task 1（theme 矩阵）→ Task 2（CSS token 依赖 data-scheme/data-mode）→ Task 3（外壳/导航）→ Task 4（首页）→ Task 5（我的页）→ Task 6（各页适配 + 终审）。

---

### Task 1: theme.ts 配色矩阵重构

**Files:**
- Modify: `src/components/theme.ts`（全部重构）
- Test: `src/components/theme.test.ts`（重写）

**Interfaces:**
- Consumes: `getSetting`/`setSetting` from `../services/api`（现有）。
- Produces:
  ```ts
  export type ThemeScheme = "sora" | "koharu" | "yuuka" | "phoebe" | "wh";
  export type ThemeMode = "light" | "dark";
  export interface Theme { scheme: ThemeScheme; mode: ThemeMode }
  export const SCHEMES: ThemeScheme[];            // 顺序固定
  export const SCHEME_NAMES: Record<ThemeScheme, string>;
  export function parseTheme(saved: string | null): Theme;
  export function applyTheme(t: Theme): void;
  export function getTheme(): Theme;
  export async function setTheme(t: Theme): Promise<void>;
  export async function initTheme(): Promise<void>;
  // 保留：getFontSize/setFontSize/applyFontSize 不变
  ```

- [ ] **Step 1: 重写测试（先红）**

```ts
// theme.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { applyTheme, getTheme, parseTheme, getFontSize, setFontSize } from "./theme";
import * as api from "../services/api";

vi.mock("../services/api", () => ({ getSetting: vi.fn(), setSetting: vi.fn() }));

beforeEach(() => {
  document.documentElement.removeAttribute("data-scheme");
  document.documentElement.removeAttribute("data-mode");
});

describe("theme matrix", () => {
  it("parses persisted value with scheme", () => {
    expect(parseTheme("koharu:dark")).toEqual({ scheme: "koharu", mode: "dark" });
  });
  it("parses legacy value as sora default", () => {
    expect(parseTheme("dark")).toEqual({ scheme: "sora", mode: "dark" });
    expect(parseTheme("light")).toEqual({ scheme: "sora", mode: "light" });
  });
  it("parses null/empty to sora light", () => {
    expect(parseTheme(null)).toEqual({ scheme: "sora", mode: "light" });
    expect(parseTheme("")).toEqual({ scheme: "sora", mode: "light" });
  });
  it("applies data-scheme and data-mode attributes", () => {
    applyTheme({ scheme: "yuuka", mode: "dark" });
    expect(document.documentElement.getAttribute("data-scheme")).toBe("yuuka");
    expect(document.documentElement.getAttribute("data-mode")).toBe("dark");
  });
  it("roundtrips getTheme from attributes", () => {
    applyTheme({ scheme: "wh", mode: "light" });
    expect(getTheme()).toEqual({ scheme: "wh", mode: "light" });
  });
  it("roundtrips font size", async () => {
    (api.setSetting as any).mockResolvedValue(undefined);
    (api.getSetting as any).mockResolvedValue("18");
    await setFontSize(18);
    expect(getFontSize()).toBe(18);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/components/theme.test.ts`
Expected: FAIL（`parseTheme` 不存在等）

- [ ] **Step 3: 重构实现**

```ts
// theme.ts
import { getSetting, setSetting } from "../services/api";

export type ThemeScheme = "sora" | "koharu" | "yuuka" | "phoebe" | "wh";
export type ThemeMode = "light" | "dark";
export interface Theme { scheme: ThemeScheme; mode: ThemeMode }

export const SCHEMES: ThemeScheme[] = ["sora", "koharu", "yuuka", "phoebe", "wh"];
export const SCHEME_NAMES: Record<ThemeScheme, string> = {
  sora: "Sora 青", koharu: "Koharu 樱", yuuka: "Yuuka 紫", phoebe: "Phoebe 橙", wh: "WH 灰",
};

export function parseTheme(saved: string | null): Theme {
  if (!saved) return { scheme: "sora", mode: "light" };
  const [rawScheme, rawMode] = saved.split(":");
  const scheme: ThemeScheme = (SCHEMES as string[]).includes(rawScheme) ? rawScheme as ThemeScheme : "sora";
  const mode: ThemeMode = rawMode === "dark" || saved === "dark" ? "dark" : "light";
  return { scheme, mode };
}

const listeners = new Set<() => void>();

export function applyTheme(t: Theme) {
  const root = document.documentElement;
  root.setAttribute("data-scheme", t.scheme);
  root.setAttribute("data-mode", t.mode);
  listeners.forEach((l) => l());
}

export function subscribeTheme(cb: () => void) {
  listeners.add(cb);
  return () => { listeners.delete(cb); };
}

export function getTheme(): Theme {
  const scheme = document.documentElement.getAttribute("data-scheme") as ThemeScheme | null;
  const mode = document.documentElement.getAttribute("data-mode") as ThemeMode | null;
  return {
    scheme: scheme && (SCHEMES as string[]).includes(scheme) ? scheme : "sora",
    mode: mode === "dark" ? "dark" : "light",
  };
}

export async function setTheme(t: Theme) {
  applyTheme(t);
  await setSetting("theme", `${t.scheme}:${t.mode}`);
}

export async function initTheme() {
  const saved = await getSetting("theme");
  applyTheme(parseTheme(saved));
}

export function getFontSize(): number {
  return Number(localStorage.getItem("reader.fontSize") ?? "18");
}

export async function setFontSize(n: number) {
  localStorage.setItem("reader.fontSize", String(n));
  applyFontSize(n);
  await setSetting("font_size", String(n));
}

export function applyFontSize(n: number) {
  document.documentElement.style.fontSize = `${n}px`;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/components/theme.test.ts`
Expected: PASS

- [ ] **Step 5: 兼容既有调用方**

`src/pages/SettingsPage.tsx` 与 `src/main.tsx` 使用旧 `Theme`（字符串）会导致 tsc 报错。本次任务把 `SettingsPage.tsx` 的主题状态改为 `{ scheme, mode }` 对象（Task 5 会整体重构该页）：

```tsx
// SettingsPage.tsx 主题部分（Step 5 修改后）
import { getFontSize, setFontSize, Theme, initTheme, setTheme, getTheme } from "../components/theme";
const [theme, setThemeState] = useState<Theme>({ scheme: "sora", mode: "light" });
useEffect(() => {
  void initTheme().then(() => setThemeState(getTheme()));
  ...
}, []);
const toggleMode = (mode: Theme["mode"]) => {
  const next = { ...getTheme(), mode };
  setThemeState(next);
  void setTheme(next);
};
// 分段控件 onClick={() => toggleMode("light")} / toggleMode("dark")；className 判断 theme.mode === "light"
```

- [ ] **Step 6: 全量测试 + 构建**

Run: `npm test`（170 绿）且 `npm run build` 通过。

- [ ] **Step 7: Commit**

```bash
git add src/components/theme.ts src/components/theme.test.ts src/pages/SettingsPage.tsx
git commit -m "refactor: 主题配色矩阵（scheme x mode）"
```

---

### Task 2: App.css MD3 token 替换 + ReaderPage.css 纸张隔离

**Files:**
- Modify: `src/App.css`（`:root`/`[data-theme]` token 块替换 + 组件样式更新 + 新增 `.side-nav` 等类）
- Modify: `src/pages/ReaderPage.css`（顶部新增 `.reader-page` 作用域纸张 token）
- Test: 无新增（CSS 视觉，构建验证）

**Interfaces:**
- Consumes: DOM `data-scheme`/`data-mode` 属性（Task 1 已设置）。
- Produces: MD3 语义 CSS 变量 `--primary`/`--on-primary`/`--primary-container`/`--surface`/`--surface-container-lowest`/`--surface-container-high`/`--on-surface`/`--on-surface-variant`/`--outline`/`--outline-variant`/`--secondary-container`/`--error` 等；`.side-nav`/`.side-nav-item` 样式类（Task 3 使用）。

- [ ] **Step 1: ReaderPage.css 顶部加纸张 token（隔离，不依赖全局）**

在 `src/pages/ReaderPage.css` 第一行之前插入：

```css
.reader-page {
  --bg: #f6f3ec;
  --bg-soft: #efeae0;
  --surface: #ffffff;
  --surface-2: #faf8f2;
  --fg: #2b2823;
  --fg-muted: #7d766a;
  --border: #e2dccb;
  --border-strong: #d3cbb6;
  --accent: #a8482a;
  --accent-hover: #8f3b22;
  --accent-soft: #f3e2d7;
  --danger: #b03a2e;
  --font-read: "Noto Serif SC", "Source Han Serif SC", "Songti SC", "STSong", "SimSun", Georgia, serif;
  --shadow-sm: 0 1px 2px rgba(66, 55, 40, 0.06);
  --shadow-md: 0 6px 24px rgba(66, 55, 40, 0.1);
  --radius-sm: 8px;
  --radius-md: 14px;
}

[data-mode="dark"] .reader-page {
  --bg: #1b1915;
  --bg-soft: #221f1a;
  --surface: #262219;
  --surface-2: #2b271f;
  --fg: #e6dfd2;
  --fg-muted: #a89e8b;
  --border: #3a3529;
  --border-strong: #4a4435;
  --accent: #d0763f;
  --accent-hover: #e0864e;
  --accent-soft: #3a2a1c;
  --danger: #d4695c;
}
```

`ReaderPage.css` 其余规则不变（继续使用这些旧变量名，现由作用域 token 提供）。`.source-reader`、`.reader-toolbar` 等已位于 `.reader-page` 之下，自动继承。

- [ ] **Step 2: 替换 App.css 顶部 token 块**

删除原有 `:root { ... }` 与 `[data-theme="dark"] { ... }` 两块，替换为 MD3 语义 token。默认块（sora 方案，双模式）给出完整值；其余四套方案的 hex 从参考库提取（路径见 Step 3）。

```css
:root,
html[data-scheme="sora"] {
  /* Sora 亮色 */
  --primary: #3b608f;
  --on-primary: #ffffff;
  --primary-container: #d4e3ff;
  --on-primary-container: #214876;
  --secondary: #545f71;
  --on-secondary: #ffffff;
  --secondary-container: #d8e3f8;
  --on-secondary-container: #3d4758;
  --tertiary: #6d5677;
  --background: #f8f9ff;
  --on-background: #191c20;
  --surface: #f8f9ff;
  --on-surface: #191c20;
  --surface-variant: #dfe2eb;
  --on-surface-variant: #43474e;
  --outline: #73777f;
  --outline-variant: #c3c6cf;
  --error: #ba1a1a;
  --on-error: #ffffff;
  --surface-dim: #d9dae0;
  --surface-container-lowest: #ffffff;
  --surface-container-low: #f2f3fa;
  --surface-container: #ededf4;
  --surface-container-high: #e7e8ee;
  --surface-container-highest: #e1e2e9;
  --shadow-sm: 0 1px 2px rgba(25, 28, 32, 0.08);
  --shadow-md: 0 6px 24px rgba(25, 28, 32, 0.12);
  --shadow-lg: 0 12px 40px rgba(25, 28, 32, 0.16);
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 22px;
  --font-ui: "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Segoe UI", system-ui, sans-serif;
  --font-read: "Noto Serif SC", "Source Han Serif SC", "Songti SC", "STSong", "SimSun", Georgia, serif;
  color-scheme: light;
}

html[data-scheme="sora"][data-mode="dark"] {
  --primary: #a5c9fe;
  --on-primary: #00315d;
  --primary-container: #214876;
  --on-primary-container: #d4e3ff;
  --secondary: #bcc7dc;
  --on-secondary: #263141;
  --secondary-container: #3d4758;
  --on-secondary-container: #d8e3f8;
  --tertiary: #d9bde3;
  --background: #111318;
  --on-background: #e1e2e9;
  --surface: #111318;
  --on-surface: #e1e2e9;
  --surface-variant: #43474e;
  --on-surface-variant: #c3c6cf;
  --outline: #8d9199;
  --outline-variant: #43474e;
  --error: #ffb4ab;
  --on-error: #690005;
  --surface-dim: #111318;
  --surface-container-lowest: #0c0e13;
  --surface-container-low: #191c20;
  --surface-container: #1d2024;
  --surface-container-high: #282a2f;
  --surface-container-highest: #32353a;
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.4);
  --shadow-md: 0 6px 24px rgba(0, 0, 0, 0.5);
  --shadow-lg: 0 12px 40px rgba(0, 0, 0, 0.6);
  color-scheme: dark;
}
```

- [ ] **Step 3: 其余四套方案的 token 块**

从参考库提取每套方案的完整 `lightColorScheme`/`darkColorScheme` 字段并映射为上述 CSS 变量名（字段名与变量名一一对应，如 `primary`→`--primary`、`onPrimaryContainer`→`--on-primary-container`、`surfaceContainerLowest`→`--surface-container-lowest`、`outlineVariant`→`--outline-variant`）。

参考库路径（每个文件含 `lightScheme = lightColorScheme(...)` 与 `darkScheme = darkColorScheme(...)` 两块）：

```
C:\Users\Administrator\AppData\Local\Temp\opencode\legado-md3\app\src\main\java\io\legado\app\ui\theme\colorScheme\KoharuColorScheme.kt
C:\Users\Administrator\AppData\Local\Temp\opencode\legado-md3\app\src\main\java\io\legado\app\ui\theme\colorScheme\YuukaColorScheme.kt
C:\Users\Administrator\AppData\Local\Temp\opencode\legado-md3\app\src\main\java\io\legado\app\ui\theme\colorScheme\PhoebeColorScheme.kt
C:\Users\Administrator\AppData\Local\Temp\opencode\legado-md3\app\src\main\java\io\legado\app\ui\theme\colorScheme\WHColorScheme.kt
```

每个文件生成两个 CSS 块：
```css
html[data-scheme="koharu"] { /* 该文件 light 值，Sora 块未覆盖的变量 */ }
html[data-scheme="koharu"][data-mode="dark"] { /* 该文件 dark 值 */ }
```
（yuka→yuuka、phoebe、wh 同理。）

- [ ] **Step 4: 更新 App.css 组件样式引用旧变量处**

在 App.css 全文中将旧变量引用替换为新 MD3 变量（保持视觉语义一致）：

| 旧 | 新 |
|---|---|
| `var(--bg)` | `var(--surface)` |
| `var(--bg-soft)` | `var(--surface-container-low)` |
| `var(--surface-2)` | `var(--surface-container-high)` |
| `var(--fg)` | `var(--on-surface)` |
| `var(--fg-muted)` | `var(--on-surface-variant)` |
| `var(--accent)` | `var(--primary)` |
| `var(--accent-hover)` | `var(--primary)`（hover 时加 `filter: brightness(0.95)` 或保留原值） |
| `var(--accent-soft)` | `var(--secondary-container)` |
| `var(--border)` | `var(--outline-variant)` |
| `var(--border-strong)` | `var(--outline)` |
| `var(--danger)` | `var(--error)` |

注意：`.book-card`/`.hit-card`/`.search-panel`/`.settings-group` 等"卡片"背景从 `var(--surface)` 改为 `var(--surface-container-lowest)`，正文背景 `body`/`.app` 用 `var(--surface)`。`--font-read`/`--font-ui`/`--radius-*`/`--shadow-*` 保留原名（已在 token 块定义）。

- [ ] **Step 5: 新增 `.side-nav` 样式（Task 3 外壳用）**

在 App.css 末尾追加：

```css
/* ============ 侧边导航栏 ============ */
.app-shell {
  display: flex;
  height: 100vh;
}
.side-nav {
  width: 96px;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  gap: 4px;
  padding: 16px 10px;
  background: var(--surface-container-low);
  border-right: 1px solid var(--outline-variant);
}
.side-nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 10px 6px;
  border: none;
  border-radius: 12px;
  background: transparent;
  color: var(--on-surface-variant);
  font-size: 12px;
  cursor: pointer;
  transition: background-color 0.18s ease, color 0.18s ease;
}
.side-nav-item:hover { background: var(--surface-container-highest); color: var(--on-surface); }
.side-nav-item.active {
  background: var(--secondary-container);
  color: var(--on-secondary-container);
  font-weight: 600;
}
.app-main {
  flex: 1;
  min-width: 0;
  overflow: auto;
  background: var(--surface);
}
.app-main .page { max-width: 980px; margin: 0 auto; padding: 0 28px 56px; }
.app-main .library-header { padding: 28px 0 8px; }
```

- [ ] **Step 6: 构建验证**

Run: `npm run build` 通过；`npm test` 170 绿。

- [ ] **Step 7: Commit**

```bash
git add src/App.css src/pages/ReaderPage.css
git commit -m "style: MD3 token 替换 + 纸张阅读页隔离"
```

---

### Task 3: App 外壳（SideNav + AppState 导航 + RssPage 占位）

**Files:**
- Create: `src/components/SideNav.tsx`
- Create: `src/pages/RssPage.tsx`
- Modify: `src/components/icons.tsx`（新增 HomeIcon、RssIcon）
- Modify: `src/App.tsx`（重构为 AppState 导航模型）
- Test: `src/App.test.tsx`（新建，外壳导航测试）

**Interfaces:**
- Consumes: `Book`/`SearchHit` 类型（现有）；`data-scheme`/`data-mode`（Task 1）；`.app-shell`/`.side-nav` 样式（Task 2）。
- Produces:
  ```ts
  // SideNav.tsx
  export default function SideNav({ area, onSelect }: {
    area: AppArea; onSelect: (a: AppArea) => void;
  })  // AppArea 从 App.tsx 导出

  // App.tsx 新增导出
  export type AppArea = "home" | "bookshelf" | "discover" | "rss" | "my";

  // RssPage.tsx
  export default function RssPage()  // 占位，无 props
  ```
- 导航模型（App.tsx 内部）：
  ```ts
  type AppState =
    | { area: AppArea }
    | { area: "detail"; page: "reader" | "sourceBook" | "sourceReader" | "explore" | "debug"; ...载荷 };
  ```
  详情页字段沿用现 App.tsx 各 View 分支的载荷（book/hit/sourceId/sourceName/chapterIndex 等）。切换到主区时记录 `from: AppArea` 用于详情返回（detail 内嵌 `back: AppArea`）。

- [ ] **Step 1: 新增图标**

`src/components/icons.tsx` 追加（复用现有 `base` 帮助函数）：

```tsx
export function HomeIcon(props: IconProps) {
  return (
    <svg {...base(props)}>
      <path d="M3 10.5 12 3l9 7.5" />
      <path d="M5 9.5V21h5v-6h4v6h5V9.5" />
    </svg>
  );
}

export function RssIcon(props: IconProps) {
  return (
    <svg {...base(props)}>
      <path d="M4 11a9 9 0 0 1 9 9" />
      <path d="M4 4a16 16 0 0 1 16 16" />
      <circle cx="5" cy="19" r="1.6" />
    </svg>
  );
}
```

- [ ] **Step 2: 新建 RssPage**

```tsx
// src/pages/RssPage.tsx
export default function RssPage() {
  return (
    <div className="rss page">
      <header className="library-header"><h1>RSS</h1></header>
      <div className="empty">
        <h2>敬请期待</h2>
        <p>RSS 订阅功能正在规划中，敬请期待。</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 新建 SideNav**

```tsx
// src/components/SideNav.tsx
import { HomeIcon, BookIcon, SearchIcon, RssIcon, SettingsIcon } from "./icons";
import type { AppArea } from "../App";

const ITEMS: Array<{ area: AppArea; label: string; Icon: (p: { size?: number }) => React.ReactNode }> = [
  { area: "home", label: "首页", Icon: HomeIcon },
  { area: "bookshelf", label: "书架", Icon: BookIcon },
  { area: "discover", label: "发现", Icon: SearchIcon },
  { area: "rss", label: "RSS", Icon: RssIcon },
  { area: "my", label: "我的", Icon: SettingsIcon },
];

export default function SideNav({ area, onSelect }: {
  area: AppArea; onSelect: (a: AppArea) => void;
}) {
  return (
    <nav className="side-nav" aria-label="主导航">
      {ITEMS.map(({ area: a, label, Icon }) => (
        <button
          key={a}
          type="button"
          className={`side-nav-item${a === area ? " active" : ""}`}
          aria-current={a === area ? "page" : undefined}
          onClick={() => onSelect(a)}
        >
          <Icon size={20} />
          <span>{label}</span>
        </button>
      ))}
    </nav>
  );
}
```

注意：若 `import type { AppArea } from "../App"` 造成循环引用（SideNav↔App），改为在 SideNav 内重复声明 `export type AppArea = ...` 并从 SideNav 导出，App.tsx 从 SideNav 导入。采用后者以避免循环依赖。

- [ ] **Step 4: 重构 App.tsx 导航模型**

```tsx
// App.tsx（Step 4 全量重构，含 detail 载荷与 back）
export type AppArea = "home" | "bookshelf" | "discover" | "rss" | "my";

type DetailState =
  | { area: "detail"; page: "reader"; book: Book; back: AppArea }
  | { area: "detail"; page: "explore"; sourceId: number; sourceName: string; back: AppArea }
  | { area: "detail"; page: "debugSource"; sourceId: number; sourceName: string; back: AppArea }
  | { area: "detail"; page: "sourceBook"; hit: SearchHit; back: AppArea }
  | { area: "detail"; page: "sourceReader"; sourceId: number; bookUrl: string; bookTitle: string; chapterIndex: number; chapterUrl: string; chapterName: string; back: AppArea };

type AppState = { area: AppArea } | DetailState;

export default function App() {
  const [state, setState] = useState<AppState>({ area: "home" });
  const area = state.area === "detail" ? state.back : state.area;

  if (state.area === "detail") {
    const go = (back: AppArea) => setState({ area: back });
    switch (state.page) {
      case "reader": return <ReaderPage book={state.book} onBack={() => go(state.back)} />;
      case "explore": return <ExplorePage sourceId={state.sourceId} sourceName={state.sourceName} onBack={() => go(state.back)} onOpenBook={(hit) => setState({ area: "detail", page: "sourceBook", hit, back: state.back })} />;
      case "debugSource": return <DebugSourcePage sourceId={state.sourceId} sourceName={state.sourceName} onBack={() => go(state.back)} />;
      case "sourceBook": return <SourceBookPage sourceId={state.hit.sourceId} sourceName={state.hit.sourceName} bookUrl={state.hit.bookUrl} initialTitle={state.hit.title} onBack={() => go(state.back)} onRead={(index, url, name) => setState({ area: "detail", page: "sourceReader", sourceId: state.hit.sourceId, bookUrl: state.hit.bookUrl, bookTitle: state.hit.title, chapterIndex: index, chapterUrl: url, chapterName: name, back: state.back })} />;
      case "sourceReader": return <SourceReaderPage sourceId={state.sourceId} bookUrl={state.bookUrl} bookTitle={state.bookTitle} initialChapterIndex={state.chapterIndex} initialChapterUrl={state.chapterUrl} initialChapterName={state.chapterName} onBack={() => setState({ area: "detail", page: "sourceBook", hit: { title: state.bookTitle, author: "", coverUrl: "", bookUrl: state.bookUrl, sourceId: state.sourceId, sourceName: "" }, back: state.back })} />;
    }
  }

  return (
    <div className="app-shell">
      <SideNav area={area} onSelect={(a) => setState({ area: a })} />
      <main className="app-main">
        {state.area === "home" && <HomePage onOpenBook={(b) => setState({ area: "detail", page: "reader", book: b, back: "home" })} onGoBookshelf={() => setState({ area: "bookshelf" })} />}
        {state.area === "bookshelf" && <LibraryPage onOpenBook={(b) => setState({ area: "detail", page: "reader", book: b, back: "bookshelf" })} />}
        {state.area === "discover" && <DiscoverPage onOpenBook={(hit) => setState({ area: "detail", page: "sourceBook", hit, back: "discover" })} onOpenExplore={(id, name) => setState({ area: "detail", page: "explore", sourceId: id, sourceName: name, back: "discover" })} />}
        {state.area === "rss" && <RssPage />}
        {state.area === "my" && <SettingsPage onOpenDebug={(id, name) => setState({ area: "detail", page: "debugSource", sourceId: id, sourceName: name, back: "my" })} />}
      </main>
    </div>
  );
}
```

配套调整（同一次 commit）：
- `LibraryPage.tsx`：`onOpenSettings`/`onOpenDiscover` props 不再使用，删除这两个 prop 的 JSX 按钮（`SettingsPage` 通过侧边栏进入）。改为可选 props 保留兼容或直接移除——测试文件 `LibraryPage.test.tsx` 只传 `onOpenBook`，移除不影响。
- `DiscoverPage.tsx`：去掉顶部「返回书架」按钮（`onBack` prop 移除；测试文件改用 `onBack={() => {}}` 会报错，需同步删除该 prop）。同时去掉 `.discover` header 的 brand，改为简洁标题。
- `HomePage`/`SettingsPage` 在后续任务实现，本任务先创建占位实现保证编译：HomePage 渲染 `<div className="page">首页</div>`，SettingsPage 沿用现页但去掉 `onBack`（见 Step 5 说明）。

- [ ] **Step 5: SettingsPage onBack 改为可选**

`SettingsPage.tsx`：`onBack` 从必选改为 `onBack?: () => void`；顶部「返回书架」按钮仅当 `onBack` 提供时渲染：

```tsx
{onBack && <button className="btn btn-ghost" onClick={onBack}>返回书架</button>}
```

- [ ] **Step 6: 新建外壳测试**

```tsx
// src/App.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import App from "./App";
import * as api from "./services/api";

vi.mock("./services/api", () => ({
  listBookSources: vi.fn().mockResolvedValue([]),
  listBooks: vi.fn().mockResolvedValue([]),
  getSetting: vi.fn().mockResolvedValue(null),
  setSetting: vi.fn().mockResolvedValue(undefined),
  getTtsRate: vi.fn().mockResolvedValue(1),
}));

describe("App shell", () => {
  it("switches areas via side nav", async () => {
    render(<App />);
    expect(await screen.findByText("首页")).toBeInTheDocument();
    await userEvent.click(screen.getByRole("button", { name: /书架/ }));
    expect(await screen.findByText(/书架空空如也/)).toBeInTheDocument();
    await userEvent.click(screen.getByRole("button", { name: /我的/ }));
    expect(await screen.findByText("设置")).toBeInTheDocument();
    await userEvent.click(screen.getByRole("button", { name: /RSS/ }));
    expect(await screen.findByText(/敬请期待/)).toBeInTheDocument();
  });
});
```

注意：若该测试因组件依赖（epubjs/pdfjs/TtsBar）在 jsdom 下报错，可改为渲染时 mock 对应模块，或在 `beforeEach` 里 `vi.mock` 掉重依赖。以实际运行为准修正。

- [ ] **Step 7: 全量测试 + 构建**

Run: `npx vitest run src/App.test.tsx` PASS；`npm test` 全绿；`npm run build` 通过。

- [ ] **Step 8: Commit**

```bash
git add src/components/SideNav.tsx src/pages/RssPage.tsx src/components/icons.tsx src/App.tsx src/pages/SettingsPage.tsx src/pages/LibraryPage.tsx src/pages/DiscoverPage.tsx src/App.test.tsx src/pages/DiscoverPage.test.tsx
git commit -m "feat: 侧边导航外壳 + AppState 导航模型"
```

---

### Task 4: HomePage（统计 + 最近阅读）

**Files:**
- Create: `src/pages/HomePage.tsx`
- Test: `src/pages/HomePage.test.tsx`（新建）
- Modify: `src/App.css`（首页样式 `.home-*`）

**Interfaces:**
- Consumes: `listBooks()`（现有）、`Book` 类型、`BookCard` 组件。
- Produces:
  ```ts
  export interface HomeStats {
    total: number;                 // 书总数
    byFormat: Array<{ format: string; count: number }>;
    openedLast7: number;           // 最近 7 天打开数（秒级时间戳）
  }
  export function computeStats(books: Book[], now?: number): HomeStats;
  export default function HomePage({ onOpenBook, onGoBookshelf }: {
    onOpenBook: (b: Book) => void; onGoBookshelf?: () => void;
  });
  ```

- [ ] **Step 1: 写失败测试**

```ts
// src/pages/HomePage.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import HomePage, { computeStats } from "./HomePage";
import * as api from "../services/api";

vi.mock("../services/api", () => ({ listBooks: vi.fn() }));

const now = 1_700_000_000;

const books = [
  { id: 1, title: "三体", format: "epub", path: "a.epub", cover_path: null, added_at: 1, last_opened_at: now - 3600 },
  { id: 2, title: "算法", format: "pdf", path: "b.pdf", cover_path: null, added_at: 2, last_opened_at: now - 10 * 86400 },
  { id: 3, title: "旧书", format: "epub", path: "c.epub", cover_path: null, added_at: 3, last_opened_at: null },
];

describe("computeStats", () => {
  it("counts total, formats, and last-7-day opens", () => {
    const s = computeStats(books, now);
    expect(s.total).toBe(3);
    expect(s.byFormat).toEqual([{ format: "epub", count: 2 }, { format: "pdf", count: 1 }]);
    expect(s.openedLast7).toBe(1);
  });
});

describe("HomePage", () => {
  it("renders stats and recent books sorted by last_opened_at", async () => {
    vi.mocked(api.listBooks).mockResolvedValue(books);
    render(<HomePage onOpenBook={() => {}} />);
    expect(await screen.findByText("3")).toBeInTheDocument();   // total
    expect(await screen.findByText("三体")).toBeInTheDocument(); // most recent
    expect(screen.getByText("算法")).toBeInTheDocument();
  });
  it("shows empty state when no books", async () => {
    vi.mocked(api.listBooks).mockResolvedValue([]);
    render(<HomePage onOpenBook={() => {}} />);
    expect(await screen.findByText(/书架空空/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/HomePage.test.tsx`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 HomePage**

```tsx
// src/pages/HomePage.tsx
import { useEffect, useState } from "react";
import BookCard from "../components/BookCard";
import { listBooks, type Book } from "../services/api";

export interface HomeStats {
  total: number;
  byFormat: Array<{ format: string; count: number }>;
  openedLast7: number;
}

export function computeStats(books: Book[], now: number = Math.floor(Date.now() / 1000)): HomeStats {
  const counts = new Map<string, number>();
  let openedLast7 = 0;
  for (const b of books) {
    counts.set(b.format, (counts.get(b.format) ?? 0) + 1);
    if (b.last_opened_at != null && now - b.last_opened_at <= 7 * 86400) openedLast7 += 1;
  }
  const byFormat = [...counts.entries()]
    .map(([format, count]) => ({ format, count }))
    .sort((a, b) => b.count - a.count);
  return { total: books.length, byFormat, openedLast7 };
}

export default function HomePage({ onOpenBook, onGoBookshelf }: {
  onOpenBook: (b: Book) => void; onGoBookshelf?: () => void;
}) {
  const [books, setBooks] = useState<Book[]>([]);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const list = await listBooks();
        if (!cancelled) setBooks(list);
      } catch (e) {
        if (!cancelled) setError(String(e));
      }
    })();
    return () => { cancelled = true; };
  }, []);

  if (error) return <div className="page"><p className="error">{error}</p></div>;

  const stats = computeStats(books);
  const recent = [...books]
    .sort((a, b) => (b.last_opened_at ?? 0) - (a.last_opened_at ?? 0))
    .slice(0, 6);

  return (
    <div className="home page">
      <header className="library-header">
        <div className="brand"><h1>你好，枕书</h1></div>
        {onGoBookshelf && <button className="btn btn-soft" onClick={onGoBookshelf}>管理书架</button>}
      </header>
      {books.length === 0 ? (
        <div className="empty">
          <h2>书架空空如也</h2>
          <p>去书架页导入书籍，开始你的阅读之旅。</p>
        </div>
      ) : (
        <>
          <div className="home-stats">
            <div className="stat-card"><span className="stat-value">{stats.total}</span><span className="stat-label">藏书</span></div>
            {stats.byFormat.map((f) => (
              <div className="stat-card" key={f.format}><span className="stat-value">{f.count}</span><span className="stat-label">{f.format.toUpperCase()}</span></div>
            ))}
            <div className="stat-card"><span className="stat-value">{stats.openedLast7}</span><span className="stat-label">近 7 天打开</span></div>
          </div>
          <h2 className="home-section">最近阅读</h2>
          <div className="book-grid">
            {recent.map((b) => <BookCard key={b.id} book={b} onOpen={onOpenBook} onRemove={() => {}} />)}
          </div>
        </>
      )}
    </div>
  );
}
```

注意：`BookCard` 的 `onRemove` 是必选 prop，首页传入空函数占位（书删除仍在书架页管理）。若视觉需要隐藏删除按钮，可给 `BookCard` 的 `onRemove` 加 `onRemove?:` 可选并条件渲染——推荐后者，见 Step 4。

- [ ] **Step 4: BookCard onRemove 改为可选**

```tsx
// BookCard.tsx 修改
export default function BookCard({ book, onOpen, onRemove }: {
  book: Book; onOpen: (b: Book) => void; onRemove?: (id: number) => void;
}) {
  ...
  {onRemove && <button className="book-remove" ... />}
}
```
相应更新 `LibraryPage.tsx` 调用处不变（仍传 onRemove），HomePage 不传。

- [ ] **Step 5: 首页样式**

`src/App.css` 追加：

```css
/* ============ 首页 ============ */
.home .library-header { padding: 28px 0 8px; }
.home-stats { display: flex; flex-wrap: wrap; gap: 12px; padding: 20px 0 8px; }
.stat-card {
  flex: 1 1 120px;
  min-width: 110px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 18px 12px;
  background: var(--surface-container-lowest);
  border: 1px solid var(--outline-variant);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);
}
.stat-value { font-size: 26px; font-weight: 700; color: var(--primary); font-variant-numeric: tabular-nums; }
.stat-label { font-size: 12px; color: var(--on-surface-variant); letter-spacing: 0.05em; }
.home-section { margin: 8px 0 0; font-family: var(--font-read); font-size: 18px; color: var(--on-surface); }
.home .book-grid { padding: 16px 0 48px; }
```

- [ ] **Step 6: 运行测试 + 构建**

Run: `npx vitest run src/pages/HomePage.test.tsx` PASS；`npm test` 全绿；`npm run build` 通过。

- [ ] **Step 7: Commit**

```bash
git add src/pages/HomePage.tsx src/pages/HomePage.test.tsx src/components/BookCard.tsx src/App.css
git commit -m "feat: 首页统计与最近阅读"
```

---

### Task 5: 我的页（SettingsPage → 单列设置中心 + 配色方案选择）

**Files:**
- Modify: `src/pages/SettingsPage.tsx`（重构为单列 MD3 设置中心）
- Test: `src/pages/SettingsPage.test.tsx`（新建）
- Modify: `src/App.css`（我的页样式 `.my-*`）

**Interfaces:**
- Consumes: `SCHEMES`/`SCHEME_NAMES`/`Theme`/`getTheme`/`setTheme`（Task 1）；`getFontSize`/`setFontSize`/`getTtsRate`/`setTtsRate`（现有）；`BookSourceManager`（现有）。
- Produces: `SettingsPage({ onOpenDebug }: { onOpenDebug?: (sourceId: number, sourceName: string) => void })`（无 onBack）。

- [ ] **Step 1: 写失败测试**

```tsx
// src/pages/SettingsPage.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import SettingsPage from "./SettingsPage";
import * as themeMod from "../components/theme";

vi.mock("../components/theme", () => ({
  SCHEMES: ["sora", "koharu", "yuuka", "phoebe", "wh"],
  SCHEME_NAMES: { sora: "Sora 青", koharu: "Koharu 樱", yuuka: "Yuuka 紫", phoebe: "Phoebe 橙", wh: "WH 灰" },
  getTheme: vi.fn().mockReturnValue({ scheme: "sora", mode: "light" }),
  setTheme: vi.fn().mockResolvedValue(undefined),
  initTheme: vi.fn().mockResolvedValue(undefined),
  getFontSize: vi.fn().mockReturnValue(18),
  setFontSize: vi.fn().mockResolvedValue(undefined),
  getTtsRate: vi.fn().mockResolvedValue(1),
  setTtsRate: vi.fn().mockResolvedValue(undefined),
}));
vi.mock("../components/TtsBar", () => ({ getTtsRate: vi.fn().mockResolvedValue(1), setTtsRate: vi.fn().mockResolvedValue(undefined) }));

describe("SettingsPage", () => {
  it("renders scheme selector and switches scheme", async () => {
    render(<SettingsPage />);
    expect(await screen.findByText("Sora 青")).toBeInTheDocument();
    await userEvent.click(screen.getByRole("button", { name: /Koharu 樱/ }));
    expect(themeMod.setTheme).toHaveBeenCalledWith({ scheme: "koharu", mode: "light" });
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/pages/SettingsPage.test.tsx`
Expected: FAIL（无配色方案选择器）

- [ ] **Step 3: 重构 SettingsPage 为单列设置中心**

```tsx
// src/pages/SettingsPage.tsx
import { useEffect, useState } from "react";
import { SCHEMES, SCHEME_NAMES, Theme, initTheme, setTheme, getTheme } from "../components/theme";
import { getFontSize, setFontSize } from "../components/theme";
import { getTtsRate, setTtsRate } from "../components/TtsBar";
import BookSourceManager from "../components/BookSourceManager";

export default function SettingsPage({ onOpenDebug }: {
  onOpenDebug?: (sourceId: number, sourceName: string) => void;
}) {
  const [theme, setThemeState] = useState<Theme>({ scheme: "sora", mode: "light" });
  const [fontSize, setFontSizeState] = useState(18);
  const [rate, setRateState] = useState(1);

  useEffect(() => {
    void initTheme().then(() => setThemeState(getTheme()));
    setFontSizeState(getFontSize());
    void getTtsRate().then(setRateState);
  }, []);

  const selectScheme = (scheme: Theme["scheme"]) => {
    const next = { ...getTheme(), scheme };
    setThemeState(next);
    void setTheme(next);
  };
  const toggleMode = (mode: Theme["mode"]) => {
    const next = { ...getTheme(), mode };
    setThemeState(next);
    void setTheme(next);
  };

  return (
    <div className="my page">
      <header className="library-header"><h1>我的</h1></header>
      <div className="my-form">
        <div className="settings-group">
          <div>
            <div className="label">主题方案</div>
            <div className="hint">选择配色方案</div>
          </div>
          <div className="segmented" role="group" aria-label="主题方案">
            {SCHEMES.map((s) => (
              <button key={s} type="button" className={theme.scheme === s ? "active" : ""} onClick={() => selectScheme(s)}>
                {SCHEME_NAMES[s]}
              </button>
            ))}
          </div>
        </div>
        <div className="settings-group">
          <div>
            <div className="label">明暗模式</div>
            <div className="hint">适应夜间阅读环境</div>
          </div>
          <div className="segmented" role="group" aria-label="明暗模式">
            <button type="button" className={theme.mode === "light" ? "active" : ""} onClick={() => toggleMode("light")}>白天</button>
            <button type="button" className={theme.mode === "dark" ? "active" : ""} onClick={() => toggleMode("dark")}>夜间</button>
          </div>
        </div>
        <div className="settings-group">
          <div>
            <div className="label">字号</div>
            <div className="hint">调节阅读正文大小</div>
          </div>
          <div className="range-row">
            <input type="range" min={12} max={32} value={fontSize} aria-label="字号"
              onChange={(e) => { const n = +e.target.value; setFontSizeState(n); void setFontSize(n); }} />
            <span className="range-value">{fontSize}px</span>
          </div>
        </div>
        <div className="settings-group">
          <div>
            <div className="label">朗读语速</div>
            <div className="hint">调节 TTS 朗读速度</div>
          </div>
          <div className="range-row">
            <input type="range" min={0.5} max={2} step={0.1} value={rate} aria-label="朗读语速"
              onChange={(e) => { const n = +e.target.value; setRateState(n); void setTtsRate(n); }} />
            <span className="range-value">{rate.toFixed(1)}x</span>
          </div>
        </div>
        <BookSourceManager onDebug={onOpenDebug} />
        <div className="settings-group">
          <div>
            <div className="label">关于</div>
            <div className="hint">枕书 · 基于 legado 3.0 规则的桌面阅读器</div>
          </div>
        </div>
      </div>
    </div>
  );
}
```

注意：`getTtsRate`/`setTtsRate` 从 `../components/TtsBar` 导入（现有），不要从 theme 模块导入（测试 mock 了两处以兼容——以 `../components/TtsBar` 为准）。

- [ ] **Step 4: 我的页样式**

`src/App.css` 追加（`.my` 复用现有 `.settings-form` 类名或新增 `.my-form`，建议 `.my-form` 与 `.settings-form` 相同的 max-width/gap）：

```css
/* ============ 我的页 ============ */
.my-form { max-width: 520px; display: flex; flex-direction: column; gap: 14px; padding: 12px 0; }
.my .settings-group { display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 14px 18px; background: var(--surface-container-lowest); border: 1px solid var(--outline-variant); border-radius: var(--radius-md); }
```

- [ ] **Step 5: 运行测试 + 构建**

Run: `npx vitest run src/pages/SettingsPage.test.tsx` PASS；`npm test` 全绿；`npm run build` 通过。

- [ ] **Step 6: Commit**

```bash
git add src/pages/SettingsPage.tsx src/pages/SettingsPage.test.tsx src/App.css
git commit -m "feat: 我的页单列设置中心 + 配色方案选择"
```

---

### Task 6: 各页 MD3 适配 + 终审

**Files:**
- Modify: `src/pages/LibraryPage.tsx`、`src/pages/DiscoverPage.tsx`、`src/pages/ExplorePage.tsx`、`src/pages/SourceBookPage.tsx`、`src/pages/SourceReaderPage.tsx`、`src/pages/DebugSourcePage.tsx`（微调 MD3 类/文案）
- Test: 各页现有测试保持绿
- 无新增逻辑，纯样式/文案适配。

**Interfaces:**
- Consumes: Task 2 的 MD3 token；Task 3 的导航（这些页面已在 App.tsx detail 分支挂好）。
- Produces: 无新接口。

- [ ] **Step 1: 检查并修正各页 MD3 视觉**

逐页核对，仅做必要微调（保持行为不变）：

1. **LibraryPage**：确认已移除「发现」按钮（Task 3 做过）。`.library-header h1` 字号、`.book-grid` 间距已由 MD3 token 覆盖；卡片背景已用 `--surface-container-lowest`（Task 2 Step 4）。无需再改。
2. **DiscoverPage**：确认已移除「返回书架」按钮（Task 3 做过）。`.discover-search input` 背景改为 `--surface-container-low`、边框 `--outline-variant`（Task 2 Step 4 已含映射，若漏则在 App.css 修正）。
3. **ExplorePage / SourceBookPage / SourceReaderPage / DebugSourcePage**：确认顶部 header 布局、按钮沿用 `.btn` 系列类即可；若某处硬编码旧颜色（如 `#fff`、`rgba`），改为 MD3 变量。
4. **正文样式（阅读正文）**：`ReaderPage.css` 已由 Task 2 的 `.reader-page` 纸张 token 隔离，不动。

检查方式：`grep -rn "var(--\|#[0-9a-fA-F]\{6\}\|rgba(" src --include=*.css` 逐个核对是否有残留旧 token 名（`--bg`/`--fg`/`--accent`/`--border`/`--surface-2`/`--danger` 等）在 App.css 之外（ReaderPage.css 除外）被引用。

- [ ] **Step 2: 全量测试 + 构建**

Run: `npm test` 全绿；`npm run build` 通过。

- [ ] **Step 3: Commit**

```bash
git add src/pages/LibraryPage.tsx src/pages/DiscoverPage.tsx src/pages/ExplorePage.tsx src/pages/SourceBookPage.tsx src/pages/SourceReaderPage.tsx src/pages/DebugSourcePage.tsx src/App.css
git commit -m "style: 各页 MD3 视觉适配"
```

- [ ] **Step 4: 终审（自审清单）**

对照设计文档 `docs/superpowers/specs/2026-08-11-md3-ui-design.md` 逐条核对：
- [ ] 5 目的地侧边栏（首页/书架/发现/RSS/我的）✓
- [ ] 5 套配色（sora/koharu/yuuka/phoebe/wh）× 明暗，设置页可切换 ✓
- [ ] 持久化 `"sora:light"` + 旧值兼容 ✓
- [ ] 阅读页纸张隔离（`data-mode="dark"` 下仍纸张）✓
- [ ] 首页统计（总数/格式/近 7 天）与最近阅读 ✓
- [ ] 我的页单列设置中心 + 配色方案 + 书源管理（含调试入口）✓
- [ ] RSS 占位 ✓
- [ ] 无新增后端命令 ✓
- [ ] `npm test` 全绿、`npm run build` 通过、工作树干净 ✓

若发现遗漏，立即修复并补 commit（`fix: MD3 终审修复`）。

---
